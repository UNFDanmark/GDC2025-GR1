using UnityEngine;

public class BrainOpenCloseScript : MonoBehaviour
{

    bool isOpen;
    
    public void OnClick()
    {
        if (isOpen)
        {
            isOpen = !isOpen;
        }
        else
        {
            isOpen = !isOpen;
        }
    }
}
